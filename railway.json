{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && cp -r dist/public server/public"
  },
  "deploy": {
    "startCommand": "NODE_ENV=production npx tsx server/production.ts"
  }
}
